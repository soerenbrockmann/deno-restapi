# Deno: RestAPI with authentication (JWT)

This app is an example RestAPI with authentication (JWT) build with [Deno](https://deno.land)

## Install

- Install [denon](https://deno.land/x/denon/)

## Run

`denon start`

## Update Cache

`deno run -r app.ts` then `denon start`
